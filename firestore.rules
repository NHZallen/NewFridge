rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 確保只有登入的使用者可以讀寫資料
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
