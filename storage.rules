rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /fridge_items/{allPaths=**} {
      // 登入用戶才可讀寫
      allow read, write: if request.auth != null
        && request.resource.size < 5 * 1024 * 1024  // 限制 5MB
        && request.resource.contentType.matches('image/.*');  // 僅允許圖片
    }
  }
}
